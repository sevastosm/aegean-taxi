export const metadata = {
  title: "About Aegean Taxi",
  description: "Aegean Taxi. The best taxi and airport transfers app to use while in Greece, Athens, Mykonos, Santorini.",
};

export default function AboutUsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
